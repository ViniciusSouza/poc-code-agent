{
  "name": "poc-code-agent",
  "version": "1.0.0",
  "description": "Coordinator code agent for modernizing target repositories to use a new gRPC client.",
  "main": "dist/index.js",
  "scripts": {
    "start": "ts-node src/index.ts",
    "build": "tsc",
    "modernize": "ts-node src/modernize.ts"
  },
  "dependencies": {
    "@octokit/rest": "^20.0.0",
    "simple-git": "^3.19.1"
  },
  "devDependencies": {
    "@types/node": "^24.2.1",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
